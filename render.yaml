services:
  - type: web
    name: threatlens-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m streamlit run threatlens/dashboard.py --server.address=0.0.0.0 --server.port=$PORT
    autoDeploy: true

